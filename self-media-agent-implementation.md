# 自媒体 Agent：模块实现蓝图（v0.1）

> 原则：先跑通共同协作，再为高频、确实省时的步骤自动化。
> 本文只定义“怎样实现”；具体的内容来源、工具账户和提醒频率须由创作者确认。

## 总体结构

```text
素材/资讯入口
      ↓
  统一素材池（可追溯、带公开权限）
      ↓
  选题 Agent（候选、质询、选题卡）
      ↓
  脚本/制作协作（版本与证据动作）
      ↓
  全平台发布记录
      ↓
  数据与反馈复盘
      └──────────→ 更新共同记忆与下一轮选题
```

共同记忆的最小落地形态建议为：Markdown 卡片 + JSONL 索引。Markdown 适合人和 Agent 共同阅读、编辑；JSONL 适合后续脚本做筛选、去重和统计。第一版不需要数据库、网页后台或多个独立服务。

---

## 模块 1：资讯与素材收集

### 1.1 要解决的事

把两种容易丢失的输入放进同一个可讨论的池子：

1. **内部一手素材**：Obsidian 中的反思与笔记、Discord 讨论、项目中的变化、以及临时闪过的想法；
2. **外部信号**：热点、行业新闻、新产品发布，以及外部讨论中出现的用户语言与内容缺口。

模块的职责是**保留原始语境、给出初步分类、提示公开风险**；它不负责自动把输入变成选题，更不替你判断热点是否值得追。

### 1.1.1 已确认的双通道材料流

| 材料流 | 已确认来源 | 典型价值 | 默认产物 |
|---|---|---|---|
| 外部变化 | 热点、行业新闻、新产品发布、外部讨论 | 提供时机、行业语言、相邻观点与内容缺口 | 信号卡 |
| 内部积累 | Obsidian、经定期沉淀的 Discord 讨论、自我反思、临时想法 | 提供一手判断、具体经历、长期命题与可展示证据 | 素材卡 |

选题模块只在两者至少有一条真实连接时，才提升为候选题：例如一个模型/产品发布触发了你此前在 Discord 或项目里反复遇到的问题；或一次自我反思恰好能反驳一个外部主流论点。外部热度和内部自说自话都不足以自动成为内容。

### 1.2 三条输入通道

| 通道 | 适合什么 | 第一版如何实现 | 何时使用 |
|---|---|---|---|
| 手动投喂 | 临时想法、自我反思、项目瞬间、链接、截图、语音转写、聊天片段 | 你在对话中粘贴，或写进 `inbox.md`；Agent 生成素材卡 | 默认、最重要 |
| 定时采集 | 经过确认的行业新闻、新产品发布、公开信息源、订阅、关键词信号 | 复用/改造现有 `topic-agent` 的采集器；按计划写入信号卡 | 信息源稳定后再开 |
| 按需检索 | 围绕一个正在讨论的选题找竞品、论点、原始来源 | 由你发起一个具体研究问题；Agent 返回带原链接的研究卡 | 选题讨论时 |

**实现边界：** Discord、Obsidian、GitHub、浏览器收藏等来源，不要一开始全部自动接入。先为每个来源声明“可读范围、公开权限、采集频率、是否需要人工挑选”，再接入。

#### Obsidian / GitHub 的实现约定（已确认）

- 权威材料源是本地 Vault：`/Users/lonky/Documents/Documents/Obsidian Vault`；
- Git 远程 `https://github.com/Lonky1995/My-vault.git` 仅用于备份、版本历史和远程恢复，**不作为第二个采集源**；
- Agent 不扫描整个 Vault，也不因文件在 Git 中存在就视为可公开；
- 第一版从 Vault 已有的 `📥 收件箱.md` 作为低摩擦人工输入入口，再由白名单目录按需读取；
- 已确认的内部白名单是：`Knowledge/`、`Daily Notes/`、`aboutme/`、`conversations/`、`Projects/`。注意实际目录名为 `conversations/`，不是 `conversation/`；
- 这五个目录全部为**可读但默认私有**：它们可以用于理解创作者、发现长期主题、找原始表达与提出候选，不得因被读取就自动进入脚本或可发布证据池；
- `aboutme/` 只承担背景画像和长期偏好，不直接作为视频中的可引用材料；`Knowledge/` 用于检索已沉淀的判断；`Daily Notes/` 与 `conversations/` 用于发现近期变化、原始表达和未加工摩擦。

#### 本地 Vault 的读取策略

| 目录 | 读取方式 | 写入素材池的条件 | 公开默认值 |
|---|---|---|---|
| `aboutme/` | 每次核心协作前读取已确认的上下文摘要；变更时刷新 | 不自动写入；只更新 Agent 对创作者的理解 | private |
| `Knowledge/` | 先建立路径、标题、修改时间、标签的轻量索引；选题讨论时按需读原文 | 只有出现一手判断、可演示项目经验或可公开论证时，生成候选素材卡 | unknown |
| `Daily Notes/` | 增量读取上次游标后的新增/修改笔记 | Agent 标出反复出现的摩擦、变化与原话，等待你确认是否提升 | private |
| `conversations/` | 增量读取上次游标后的新增/修改对话 | Agent 提取你反复使用的语言、未解决的问题与决定变化，等待你确认 | private |
| `Projects/` | 增量读取项目文档、变更摘要与可公开的实现记录；不默认读取密钥、部署配置或全部源码 | 每次关键改动先被转为“问题—设计变化—产品洞见—证据—公开风险”的项目变化卡，再由你决定是否提升为素材 | private |

初始阶段不应把 `Knowledge/` 全文喂给模型。先建立索引，在一个具体主题被讨论时检索相关笔记；这既降低噪音，也避免把不相关的私密历史带进每一次内容讨论。

#### Discord 的处理方式（已确认）

不直接接入 Discord API，不抓取频道原始消息。Discord 中有价值的讨论已经定期沉淀到 Vault 的 `conversations/`，并进入 `aboutme/` 的长期上下文；内容 Agent 应读取这些**经过整理的二级材料**。

这样做的收益是：减少噪音、避免把即时私聊/频道内容误作公开素材、保留你已经形成的判断。代价是它无法实时捕捉某条新对话；如果将来需要追溯，素材卡应保留对应的 Vault 路径，而不是默认拉取 Discord 原文。

#### 项目变化卡：把开发改动转为内容线索

以 LonkyClaw 的一次优化为例，项目模块不应输出“本周新增了记忆模块/改了 N 个文件”，而应先形成：

```text
触发：我原来想解决的真实问题是什么？
旧方案：之前怎样做，哪里不够？
这次变化：产品机制或协作规则改变了什么？
用户/使用者价值：它减少了什么摩擦，或让什么判断成为可能？
可抽象的洞见：这一变化对人和 Agent 协作说明了什么？
一手证据：可演示的前后对照、日志、界面或原始对话是什么？
公开风险：哪些实现、数据、场景必须删掉或脱敏？
下一步：归档 / 进入选题讨论 / 仅作为未来证据
```

这样，`Projects/` 不是“自动把 commit 变成视频”的输入，而是主动寻找**项目变化背后的产品判断**。LonkyClaw 的记忆、沟通、反馈和共同迭代机制，都可以从这里被发现并进入选题讨论。

#### 周度项目变化雷达（已启用）

每周一上午，线程内的自动化会检查 `Projects/` 自上次运行后的变化，并输出不超过 5 张项目变化卡。它必须说明原始材料路径、问题、旧方案缺口、设计变化、可抽象洞见、公开边界与建议下一步；没有足够强的变化时明确不凑数。

这份周报只负责提出“值得讨论什么”，不能替你把项目变化升级成选题，也不应根据代码改动数量判断内容价值。你从周报中选中一张卡后，才进入模块 2 的选题讨论。

### 1.3 统一素材卡数据结构

每次输入最终都落为一张可读的 Markdown 卡，并在 `data/materials.jsonl` 追加一条索引。

```yaml
id: mat-YYYYMMDD-短标识
captured_at: ISO 时间
origin: manual | scheduled | research
source_type: project | conversation | article | video | comment | news | note
source_ref: 原链接或本地文件路径
raw_excerpt: 原话或内容摘要
public_status: public | redaction_required | private | unknown
topics: []
related_content_ids: []
status: inbox | reviewed | promoted | archived
```

`raw_excerpt` 必须尽量保留原话，不让 Agent 过早总结成“正确但没有用”的结论。`public_status` 是硬字段：没有明确为 `public` 或 `redaction_required` 的素材，不得进入脚本证据池。

### 1.4 Agent 在每次收集时做什么

```text
1. 复述原始材料，不改变其含义；
2. 识别：这是内部素材、外部信号，还是暂时噪音；
3. 标出可验证的事实、个人判断与未知项；
4. 检查公开边界；
5. 给一个下一步：归档 / 补充信息 / 进入选题讨论。
```

它不能做：把一段项目日志直接改写为视频脚本、把私有材料默认为可公开、把外部热度当作内容价值。

### 1.5 模块 1 与现有 topic-agent 的对接

| 已有能力 | 如何保留 | 需要补的能力 |
|---|---|---|
| 外部信号快照、去重、候选选题 | 保持在 `/Users/lonky/Projects/lonky-site/scripts/topic-agent` | 信号输出同步到统一素材池，而不只写单次报告 |
| `config.ts` 的公开素材与边界 | 作为初始账号记忆 | 改为从共同记忆读取已确认的账号契约与素材卡 |
| `feedback.jsonl` | 继续记录人工选择与发布结果 | 扩展内容 ID、平台与高质量反馈字段，和复盘模块关联 |

第一阶段不要求修改现有代码。先以文件约定验证字段和使用频率；等手动投喂与选题讨论稳定后，再改造 TypeScript 数据结构，避免把未验证的流程写死。

### 1.5.1 外部数据源盘点（2026-08-30）

现有 `/Users/lonky/Projects/lonky-site/scripts/topic-agent/sources.ts` 只定义了两种采集器：

| 现有采集器 | 实际收集什么 | 当前作用 | 限制与状态 |
|---|---|---|---|
| `douyin-hot` | 抖音总热榜中的热词与热度 | 捕捉广义公共讨论的时机 | 泛娱乐噪音高；需要登录态与浏览器；尚无运行快照验证稳定性 |
| `xhs-search` | 小红书中预设关键词的笔记标题及赞/藏/评合成热度 | 观察 AI 产品/工作流相关内容如何被表达 | 只覆盖 6 个旧关键词；需要登录态与浏览器；尚无运行快照验证稳定性 |

当前关键词为：`AI 产品经理`、`AI 工作流`、`AI Agent 实践`、`AI 自动化 业务`、`产品经理 AI 编程`、`AI 工具 做出来 没人用`。

**当前没有接入：** 行业新闻、模型/产品发布、公司官方更新、GitHub/开源项目变化、权威技术论文/博客、中文深度文章或你关注创作者的更新。因此现有实现只能叫“内容平台信号层”，不能叫“外部行业雷达”。

### 1.5.2 外部雷达应拆成三层

| 层 | 要回答的问题 | 建议源类型 | 进入选题前的要求 |
|---|---|---|---|
| 一手变化 | 发生了什么可核验的变化？ | 公司官方发布、产品更新日志、官方博客、GitHub Releases、论文原文 | 保留原链接与发布日期 |
| 行业解释 | 这件事为什么值得关注？不同人怎么理解？ | 可信媒体、研究者/产品从业者的深度分析、播客/文章 | 与一手变化分开记录，标明是解释而非事实 |
| 平台语言 | 目标人群会怎样描述问题？哪些表达已同质化？ | 抖音/小红书搜索、评论区、相邻创作者内容 | 不当作事实源；只用于发现用户语言和内容缺口 |

后续实现时，三层分别采集、分别打标签；任何一条内容若引用新闻或发布，必须回到一手变化链接核验。抖音热榜和小红书搜索保留，但只放在第三层，不能主导选题。

### 1.5.3 参考 Zara 的 AI 学习库：新增“学习库”而非再加一个热榜

[Zara's AI learning library](https://zara.faces.site/ai) 的结构将长视频、播客、构建者、新闻通讯、实际使用的产品和可复用 Skills 分开管理；每条精选都有策展人的个人理由。它提供的关键启发不是“把她的列表全抄进来”，而是：**外部输入必须按用途分层，且人工判断应保留在来源旁边。**

对本项目，外部材料应分成两个不同系统：

| 系统 | 回答什么 | 更新方式 | 是否自动推送 | 典型产物 |
|---|---|---|---|---|
| 变化流（Radar） | 最近发生了什么，是否值得现在讨论？ | 定时抓取一手变化与少量行业解释 | 是，但只推高相关变化 | 信号卡 |
| 学习库（Library） | 哪些材料长期值得反复学习、会影响我的判断？ | 你手动标记或 Agent 在讨论后建议收录 | 否；按主题检索或周度回顾 | 学习卡 |

你已经有本地 Vault 的 `Knowledge/`，因此这里**不新建第二套学习库**。Learning Library 应作为 `Knowledge/` 的一个内容 Agent 视图：外部材料只有在你确认它会长期影响判断后才归档到已有知识库；未确认的链接仍留在变化流或待处理素材池。

归档时建议按“类型 + 为什么值得”保存，而不是按链接堆积：

```text
类型：产品案例 / Agent 协作 / AI Native 组织 / 模型与工程 / 创作者表达 / 增长与分发
来源与原链接：
我为什么保留它：
它改变或挑战了我的哪个判断：
可连接的内部素材或项目：
可转化的内容线索（如果有）：
```

来源准入原则借鉴其“follow builders, not influencers”的思路，但不能机械套用：优先收录实际在做产品、研究或一线实践的人；对纯观点型内容，记录它提出了什么问题，不把它当作事实或权威结论。

因此，Zara 页面上的新闻通讯、播客、公开视频、人物和产品可以作为**候选来源池**，等你确认与你的长期主题相关后才纳入；不把她的完整清单直接自动订阅，也不把“我喜欢”误认为“我的受众需要”。

### 1.6 模块 1 的验收标准

连续两周后，以下都成立才算它真正跑通：

- 你能在一分钟内把一条素材放进池子；
- 每条被使用的素材都能回溯原始来源与公开边界；
- 选题讨论时，Agent 能引用至少一条内部一手材料，而不只输出热点；
- 被否决或暂缓的素材也保留原因，下一次不会重复推给你；
- 没有任何私密内容被默认进入可发布材料。

### 1.7 待你确认的接入清单

| 来源 | 你是否希望接入 | 接入方式：自动/手动/按需 | 可读范围 | 公开默认值 |
|---|---|---|---|---|
| Obsidian（本地 Vault） | 是 | `📥 收件箱.md` 手动投喂；`Knowledge/`、`Daily Notes/`、`aboutme/`、`conversations/`、`Projects/` 按目录策略读取 | 已确认白名单 | private/unknown；需逐条确认公开状态 |
| GitHub（My-vault） | 是，但仅备份/历史 | 不采集 | 不适用 | 不适用 |
| Discord | 是 | 待确认 | 待确认频道与时间范围 | unknown；默认不可公开 |
| GitHub / 项目日志 | 待确认 | 待确认 | 待确认 | 待确认 |
| 网页与文章收藏 | 待确认 | 待确认 | 待确认 | 待确认 |
| 行业新闻 / 新产品发布 / 热点 | 是 | 定时 + 按需 | 公开来源，待确认具体清单 | public（仍需事实核验） |
| 临时想法 / 自我反思 | 是 | 手动投喂 | 由你主动提供 | unknown |

---

## 模块 2–5：后续实现顺序

| 模块 | 依赖 | 第一版实现方式 | 进入自动化的前提 |
|---|---|---|---|
| 选题讨论与选题卡 | 统一素材池、账号契约 | 改造已有 topic-agent + 对话式质询 + Markdown 选题卡 | 连续多轮使用后，字段稳定且人工选择理由可归纳 |
| 脚本与制作 | 已确认的选题卡、可公开证据 | 对话式陪练 + 脚本版本卡 + 发布检查单 | 你确认哪些脚本步骤高度重复、不会损害表达 |
| 发布记录 | 成片与平台链接 | 一张内容链路卡，手动记录三端发布版本 | 账号后台或导出能稳定获取且你确实愿意维护 |
| 数据、反馈与复盘 | 发布记录、后台数据、评论原话 | 手动上传/粘贴 + Agent 对照复盘 + 反馈卡 | 数据字段稳定、连续多条内容后出现可比较模式 |

### 模块 2：选题讨论与选题卡

#### 已确认的触发机制

| 触发方式 | 何时发生 | Agent 读取什么 | 产出 |
|---|---|---|---|
| 固定周更 | 每周一次，在项目变化雷达之后 | 本周项目变化卡、Vault 新材料、现有外部信号、已发布内容与反馈 | 一批待讨论的选题研究包，而不是直接推荐选题 |
| 手动触发 | 你带来一个链接、项目改动、观察或模糊想法时 | 该材料及其相关内部素材；必要时按需外部检索 | 一个主题的选题研究包；之后再共同讨论 |

固定周更不是“每周强制发一条”，而是固定做一次候选话题整理；手动触发不等待周报，保留你在真正有感觉时的创作主动性。

每周固定回顾已通过线程内 heartbeat 自动化启用，运行时间为每周一上午。它将原“每周项目变化雷达”合并为“每周自媒体项目与选题回顾”：先产出项目变化卡，再生成候选题，二者共用同一份材料与公开边界；不会分别运行两套脱节的周报。

#### 核心原则：先强信息，后弱建议

选题模块分成两个阶段，不能跳过第一阶段：

```text
阶段 A：选题研究包
事实、来源、已有角度、内容缺口、与你的材料和历史反馈的连接
        ↓
阶段 B：人和 Agent 的深入讨论
质询、比较、反驳、收窄角度、决定做/不做/暂缓
        ↓
阶段 C：已确认选题卡
只有经你确认后，才进入脚本与制作
```

阶段 A 的目标是**强信息、弱建议**。Agent 可以提出候选角度，但不输出“应该做第 1 名”或用一个不透明总分替你决定。阶段 B 才允许围绕“这题为什么值得占用一次公开表达机会、又为什么不值得”深入讨论。

#### 研究证据规则：每个点都要能追溯

选题研究包不是摘要，而是可回看的证据包。每个事实、样本、判断和候选角度都必须带来源，并且来源要尽量靠近一手信息。

来源可以是：

- 外部原链接：官方发布、产品更新、文章、视频、播客、平台搜索结果页；
- 本地材料路径：Vault 笔记、`Projects/` 项目记录、整理后的 `conversations/`、`aboutme/` 中的上下文；
- 自身反馈记录：发布链接、后台数据截图/导出、评论原话、私信或同行反馈记录；
- 手动输入：你当场提供的观察、链接、截图说明或口述材料。

没有来源的内容必须标为“待补来源”。待补来源只能进入“问题”“假设”“还缺什么”，不能进入“事实”“主流结论”“热度判断”或“推荐角度的支持证据”。

来源质量按以下顺序处理：

```text
一手事实 > 可核验样本 > 平台表达/评论 > 二手解读 > Agent 推断
```

平台作品数、浏览量、点赞、收藏、评论等可以用来判断讨论热度，但不能单独证明“用户真的有需求”；评论区里的具体问题和重复追问，权重高于单纯互动量。

#### 角度地图的平台采样规则

第一版优先看与你发布场景一致的三个中文平台：

| 平台 | 每个关键词采样量 | 第一版目标 | 数据获取方式 | 风险 |
|---|---:|---|---|---|
| 抖音 | 10–20 条 | 看短视频语境下什么标题、冲突和表达方式更容易被消费 | 优先复用现有 `topic-agent` 的登录态浏览器采集；必要时手动搜索补样本 | 热榜噪音高；关键词搜索和评论能力不稳定，官方权限需要申请 |
| 小红书 | 10–20 条 | 看用户如何把问题说成“自己的困惑”，尤其收藏、评论和标题承诺 | 现有 `xhs-search` 原型已能按关键词抓笔记标题和互动数；必要时手动打开代表笔记 | 平台接口易变；互动数不等于深度需求 |
| 视频号 | 10–20 条 | 看微信熟人/半熟人内容场里，什么议题更适合建立信任 | 第一版先手动搜索、截图或记录链接；暂不承诺自动化采集 | 缺少稳定公开采集路径，优先作为人工样本源 |

10–20 条不是为了统计显著性，而是为了快速看清“大家在怎么讲”。每个平台的样本先按关键词取 Top/高互动/最近发布的代表内容，再由 Agent 归纳角度，不用追求全量。

每个平台至少输出：

```text
1. 代表内容列表：标题、链接、发布时间、可见互动数据、source_id
2. 角度聚类：这些内容分别在讲什么
3. 高频承诺：标题/开头反复给观众什么承诺
4. 评论问题：观众具体在追问什么；没有评论样本就写未知
5. 拥挤判断：哪些表达已经同质化
6. 空位判断：哪些问题有需求信号但没有被讲透
```

数据拿不到时，Agent 不能跳过这一层直接给结论，而要把该平台标为“采集失败/手动待补”，并说明失败原因。能人工补 10 条高质量样本时，优先人工补，不为了自动化牺牲判断质量。

#### 选题研究包：固定输出结构

每个待讨论主题先生成如下研究包：

```text
0. 主题定义
   这次研究的到底是什么问题，而不是一个宽泛关键词？

1. 整体热度 / 需求信号
   - 搜索、讨论、产品发布、内容互动或重复提问的事实
   - 每个信号对应的来源、时间范围、指标口径
   - 信号是上升、持续还是一次性；没有可靠数据必须明确写“未知”

2. 现有角度地图
   - 已有内容通常在讲什么？代表链接与发布时间是什么？
   - 常见叙事、主流结论、工具化/营销化表达分别是什么？
   - 哪些角度已经拥挤，哪些问题没有被说清？

3. 与我的连接
   - 关联的内部项目、反思、对话或可公开的一手证据
   - 相关的历史作品与实时反馈：数据、评论原话、私信/同行对话
   - 与账号契约是否一致；若不一致，明确说明

4. 对我可能成立的角度（弱建议）
   - 给 2–4 个候选角度，而不是单一结论
   - 每个角度写：为何可能适合我、证据来源、风险/反例来源、还缺什么

5. 讨论问题
   - 哪个判断我真的愿意公开承担？
   - 哪一条证据最能证明，而不是装饰观点？
   - 哪个角度即使数据高，也不该做？
```

热度与“适合你”必须分开。热度只能证明外部注意力或需求信号，不能证明你应讲；历史数据和评论只能校准候选角度，也不能让一次高播放静默改写定位。

#### 周度研究包的输出约束

每周最多生成 5 个待讨论主题的研究包，至少覆盖：

```text
1 个来自内部项目/反思的常青题
1 个内部材料与外部变化相交的时效题（若本周有可靠外部变化）
1 个明确标记为“暂缓/证据不足”的候选，保留否决原因
```

研究阶段不产出最终标题、脚本或“推荐第一名”。若没有足够可靠的主题，少于 5 条优于凑数；证据不足的主题必须保留为暂缓，而不是由模型补全。

### 模块 1 当前状态：最小可用，外部雷达持续迭代

已确定：内部材料从本地 Vault 的白名单读取；Discord 通过整理后的 `conversations/` 与 `aboutme/` 间接进入；`Projects/` 每周输出一次值得讨论的项目变化；现有抖音/小红书信号层和手动投喂的外部链接可作为外部输入。

未完成但不阻塞后续：行业新闻、产品发布、RSS/X List/newsletter 和开源 Release 的一手变化源。它们通过 [`external-source-registry.md`](./external-source-registry.md) 持续补充，不需要在开始模块 2 前一次性接完。

下一步进入模块 2：选题讨论与选题卡的实现。
